# Decision Card: Proof Module Metrics Confirmation

**Date**: January 8, 2026  
**Status**: Awaiting Approval  
**Context**: The approved CTA+Trust proposal included a "Live Proof Snapshot" module showing anonymized counts (Decision Cards 7d, Verified Claims 30d, Active Counties, Median time-to-decision). Per approval constraint: **"no new API endpoint ships unless the metric is already queryable today."**

---

## Current State

- Landing page has new CTA "Start with Scout" + trust bullets
- Proof module was **explicitly deferred** until metrics sources are confirmed
- No metrics endpoint exists yet
- No proof module UI component exists yet

---

## Goal

Determine which metrics (if any) are queryable today, and whether to ship the proof module now or defer until VAC audit is complete.

---

## Metrics Inventory (Needs Confirmation)

Before implementing any proof module, Thomas must confirm for **each metric**:

1. ✅ **Is the table/data source real?** (not placeholder)
2. ✅ **Can it be queried safely?** (performant, no PII leak)
3. ✅ **Is the metric accurate?** (matches user-facing claim)

---

### Metric 1: Decision Cards Processed (Last 7 Days)

**Claim**: "X Decision Cards processed (last 7 days)"

**Query assumption**:
```sql
SELECT COUNT(*) 
FROM decision_cards 
WHERE created_at >= NOW() - INTERVAL '7 days';
```

**Thomas must confirm**:
- [ ] Table `decision_cards` exists and is populated
- [ ] `created_at` field exists and is accurate
- [ ] Count reflects actual Decision Cards generated by Scout (not test data)
- [ ] Query is performant on production dataset

**If NO**: Omit this metric entirely (no "0", no placeholder)

---

### Metric 2: Verified Claims (Last 30 Days)

**Claim**: "X Verified claims (last 30 days)"

**Query assumption**:
```sql
SELECT COUNT(*) 
FROM verified_claims 
WHERE verified_at >= NOW() - INTERVAL '30 days'
  AND status = 'verified';
```

**Thomas must confirm**:
- [ ] Table `verified_claims` exists and is populated (or equivalent tracking exists)
- [ ] `verified_at` field exists and is accurate
- [ ] Count reflects actual VAC verifications (not signup count, not pending/rejected)
- [ ] Query is performant on production dataset

**If NO**: Omit this metric entirely (no "0", no placeholder)

**Conditional phrasing**: If count > 0 AND VAC is active in ≥5 counties → show metric. Otherwise omit.

---

### Metric 3: Active Counties

**Claim**: "X Active counties"

**Definition assumption**: Counties with at least 1 verified professional or active Decision Card in last 30 days.

**Query assumption**:
```sql
SELECT COUNT(DISTINCT county_id)
FROM users
WHERE verification_status = 'verified'
  AND county_id IS NOT NULL;
```

**Thomas must confirm**:
- [ ] Table `users` has `county_id` and `verification_status` fields
- [ ] Count reflects real activity (not just signup geography)
- [ ] "Active" definition matches user-facing claim
- [ ] Query is performant on production dataset

**If NO**: Omit this metric entirely (no "0", no placeholder)

---

### Metric 4: Median Time-to-Decision (Optional)

**Claim**: "Median response time: X hours" or "Median time-to-decision: X minutes"

**Query assumption**:
```sql
SELECT PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY time_to_decision)
FROM (
  SELECT EXTRACT(EPOCH FROM (dc.created_at - s.started_at)) / 60 AS time_to_decision
  FROM decision_cards dc
  JOIN scout_sessions s ON dc.session_id = s.id
  WHERE dc.created_at >= NOW() - INTERVAL '30 days'
) AS times;
```

**Thomas must confirm**:
- [ ] Tables `decision_cards` and `scout_sessions` exist and are linked
- [ ] Session start time is tracked accurately
- [ ] Median calculation is meaningful (n ≥ 20 samples)
- [ ] Query is performant on production dataset

**If NO**: Omit this metric entirely (default in IMPLEMENTATION_DEFAULTS.md)

---

## Options

### Option A: Confirm Metrics Now and Implement Proof Module

**What**: Thomas confirms which metrics pass all criteria → we implement the proof module with only valid metrics.

**Requirements**:
- Thomas provides exact table names, column names, and sample queries for each valid metric
- We implement `server/analytics/proof-metrics.ts` endpoint (read-only, cached, rate-limited)
- We implement `client/src/components/ProofSnapshot.tsx` component
- Proof module shows **only** metrics that pass validation
- If no metrics pass → hide proof module entirely (no "Coming soon", no placeholders)

**Timeline**: 1–2 hours after confirmation

**Pros**:
- Delivers full approved vision (CTA + trust + proof)
- Social proof increases trust and signup conversion
- Metrics are real and verifiable

**Cons**:
- Requires Thomas to audit tables/queries before approval
- Adds API endpoint (minimal risk, but not zero)

---

### Option B: Show "Coming Soon" Placeholder (No Metrics)

**What**: Implement proof module UI with "Coming soon" text and no numbers.

**Example**:
```
Live Proof Snapshot
📊 Proof metrics coming soon
Counts are anonymized; contact requires a Decision Card.
```

**Pros**:
- Delivers UI presence without risking fake/inaccurate numbers
- Still reinforces "proof-first" message
- Zero query risk

**Cons**:
- Violates "no placeholders" rule from METRICS_READINESS_CHECKLIST.md
- Weakens trust (promises proof but doesn't deliver)
- May feel incomplete

---

### Option C: No Proof Module Until VAC Audit Complete

**What**: Do not ship proof module at all until Thomas confirms VAC coverage and metric accuracy.

**Rationale**: The CTA + trust framing is already complete. Proof metrics are optional enhancement, not core to the authority frame.

**Pros**:
- Zero risk of fake/inflated numbers
- Keeps commitment to "counts-only, verified only"
- Landing page trust framing is sufficient without proof module

**Cons**:
- Misses opportunity for social proof
- Approved design is incomplete (proof module was part of the proposal)

---

## Recommendation

**Option A** (confirm metrics now) **IF** Thomas can quickly verify at least 2 metrics are queryable today.

**Option C** (no proof module) **IF** metrics sources are unconfirmed or VAC audit is pending.

**Never Option B** (placeholder text violates "no fake success" rule).

---

## Approval Required

Choose one:

- [ ] ✅ **Approve Option A** (confirm metrics below and implement proof module)
- [ ] ✅ **Approve Option C** (no proof module until VAC audit complete)
- [ ] ❌ **Request edits**

---

## If Option A Chosen: Metrics Confirmation Checklist

For each metric Thomas approves, provide:

1. **Table name**: (e.g., `decision_cards`)
2. **Column names**: (e.g., `created_at`, `status`)
3. **Sample query**: (exact SQL to run)
4. **Expected count range**: (e.g., "50–200 per week")

**Example**:

```yaml
Decision Cards (7d):
  table: decision_cards
  columns: created_at, id
  query: SELECT COUNT(*) FROM decision_cards WHERE created_at >= NOW() - INTERVAL '7 days'
  expected_range: 100-500
  approved: true

Verified Claims (30d):
  table: user_verifications
  columns: verified_at, status
  query: SELECT COUNT(*) FROM user_verifications WHERE verified_at >= NOW() - INTERVAL '30 days' AND status = 'verified'
  expected_range: 50-200
  approved: true

Active Counties:
  table: users
  columns: county_id, verification_status
  query: SELECT COUNT(DISTINCT county_id) FROM users WHERE verification_status = 'verified' AND county_id IS NOT NULL
  expected_range: 10-30
  approved: true

Median Time-to-Decision:
  approved: false  # Omit until scout_sessions tracking is confirmed
```

---

## Implementation Plan (If Option A + Metrics Confirmed)

1. Create `server/analytics/proof-metrics.ts` with validated queries
2. Add Redis cache layer (5-minute TTL)
3. Create `client/src/components/ProofSnapshot.tsx` component
4. Add proof module to landing page hero (below trust bullets)
5. Write unit tests for metrics endpoint
6. Test with all metrics null (should hide entirely)
7. Test with partial metrics (should show only valid)
8. Add monitoring/alerting if metrics query fails
9. Run forbidden-patterns check
10. Commit as "Proof module: live anonymized counts (metrics confirmed)"

**Estimated time**: 1–2 hours  
**Risk**: Low (read-only queries, cached, rate-limited)

---

## Fail-Safes

1. ✅ Never show zero or placeholder
2. ✅ Hide entire module if no metrics pass validation
3. ✅ All queries are read-only aggregates (no PII)
4. ✅ Cache layer prevents DB hammering
5. ✅ Rate limiting on public endpoint (100 req/min per IP)
6. ✅ Error handling: on query failure → log error + return empty array (hides module)

---

## Notes

- If VAC audit reveals that Verified Claims metric is inaccurate, we can disable just that metric via feature flag without removing the entire module
- Proof module is optional; CTA + trust framing is complete without it
- Thomas can approve Option C now and revisit Option A later after VAC audit
