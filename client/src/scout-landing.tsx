import React from "react";

export default function ScoutLanding() {
  return (
    <div style={{ padding: 40, color: "white" }}>
      <h1>Scout Landing Minimal Test</h1>
      <p>If you see this, the file compiles successfully.</p>
    </div>
  );
}
import React from "react";

// Minimal stub so Vite sees a valid module with a default export.
// You can replace this with the full Scout landing implementation.
export default function ScoutLanding() {
  return <div />;
}
