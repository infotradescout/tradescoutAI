/**
 * Reusable premium service-first portfolio for photographers and drone operators.
 *
 * Precision Aerial established this presentation contract: a normal website hero
 * combined with public-profile identity, full-bleed owned media, selected work,
 * service booking, social links, trust actions, and the TradeScout handoff.
 * Keep the compatibility implementation in one place while new profiles select
 * the shared `photographer-drone` template instead of receiving slug-specific UI.
 */
export { default } from "./PrecisionAerialProfile";
