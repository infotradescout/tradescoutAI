import EmptyState from "@/components/EmptyState";

const SavedContractors = () => {
  return <EmptyState title="Saved Contractors" message="No data available yet." />;
};

export default SavedContractors;
