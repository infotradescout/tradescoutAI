// Legacy reference for the old "Millionaire" Scout landing.
// The full historical implementation lives in git history under
// `client/src/scout-landing-v2-millionaire.tsx`.
//
// The active entry experience is now the hybrid OS landing in
// `client/src/scout-landing.tsx`. This stub exists only so we
// remember the previous variant without shipping 1,700+ LOC.

export {};
