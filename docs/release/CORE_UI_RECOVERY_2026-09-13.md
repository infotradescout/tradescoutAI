# Core UI release recovery — 2026-09-13

PR #647 already contains the HomeID record and package redesign, canonical property editor, and saved-data truth repairs. This recovery preserves that work and merges current `main` (`e34f02520c697ee8e52fefcf6ebdd4d06ce6d6c2`) locally. It does not change custom business profiles or publish a release.

## Concrete blocker and correction

The existing [hosted shell report](https://tradescout-core-ui-proof-647.onrender.com/report.json) at `0d40a2682c9cd3c8c21fc822c0689508e39ad898` records `M client/public/sitemap-index.xml` after its successful build. The same commit's [strict release report](https://tradescout-core-ui-proof-647.onrender.com/minimum-release-report.json) fails the initial clean-tree requirement. The generator stamped every child sitemap with the current build date, so a later-day build changed tracked source and falsely implied updated content.

- The canonical generator preserves recorded route dates and omits unknown dates for new routes and dynamic child sitemaps. All 56 route targets and 17 child sitemap targets remain. Google defines child `lastmod` as optional and tied to the actual child file modification time: [sitemap index reference](https://developers.google.com/search/docs/crawling-indexing/sitemaps/large-sitemaps).
- The 17 removed date lines reduce the reviewed public source total by exactly 578 bytes, from 2,801,062 to 2,800,484. The existing manifest and validator advance together to version 5; the 200-file count, media identities, aliases, digests and build budgets remain pinned. The first corrected build properly failed until this exact source-byte change was recorded.
- The six existing core UI proof scripts now default to `test-results/core-ui-proof`, the repository's existing ignored test-output area. Explicit `CORE_UI_PROOF_DIR` still works. Reports no longer create an untracked source input under the default local invocation. The strict gate's source and assertions are unchanged.

## Executed verification

The machine used Linux x64 and Node 24.19.0 with `npm ci --include=dev`.

| Check | Result |
| --- | --- |
| Full TypeScript on merged core UI source | Pass |
| Core UI, HomeID, navigation and Direct Connect contracts | 296 passed, 22 suites |
| Actual generator across midnight and ten years later | Three regression cases failed before correction; all three pass after |
| Existing sitemap, Exchange SEO and profile SEO contracts | 75 passed, five suites |
| Existing public-shell identity and normalization contracts | Nine passed |
| Full corrected production client/server build | Pass, including unchanged asset budgets and media guards |
| Source status after corrected build | Clean |
| Six proof script syntax checks and ignored-output check | Pass |

The regression executes the actual generator in disposable fixture directories, changes the process clock, and compares generated bytes. It also verifies preservation of recorded dates and omission of unknown dates. The build runs these tests through the existing prebuild owner.

Machine-readable scope and log hashes: [validation.json](evidence/coreui-recovery-2026-09-13/validation.json). Core UI tests and typecheck ran on merge `815b4e1d1aee5f72b90064ff18fa8e9d420c9fd7`; subsequent source changes are the generator, reviewed public output/manifest, and proof-output defaults. The final corrected build passed on those changes. This is not a strict release attestation for the final commit.

## Remaining acceptance

Historical hosted evidence at `0d40a268` passed 11 shell checks, eight overview checks, 26 record/package checks, six native property-edit groups and 18 desktop/mobile property-edit browser checks. Those results belong to that earlier commit. Its strict release gate failed and is not represented as green.

Run `npm ci --include=dev && node scripts/verify-core-ui-upgrade.mjs` from a clean checkout of the exact combined candidate in the existing isolated proof environment. The native PostgreSQL fixture requires non-root Linux; this workspace cannot drop privileges. Chromium also failed before creating a page. No new-candidate native database, browser, strict release gate or production acceptance is claimed here. No external source push, merge, hosted mutation, production database write, customer action or provider operation was performed for this recovery.
