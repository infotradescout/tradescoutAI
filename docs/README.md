This folder contains TradeScout documentation, specs, deployment guides, and status reports.

The application code lives primarily under `client/` and `server/`.
Moving standalone Markdown files here reduces noise in the repo root
without affecting runtime behavior.
