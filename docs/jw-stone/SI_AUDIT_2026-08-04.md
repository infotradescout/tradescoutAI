# SI Audit — JW Stone / TradeScout (2026-08-04)

**Mode:** Selective Intelligence verification pass (intake → objector → aligner → verifier).  
**Not a feature build.** Owner chat = product authority.  
**Repo:** `TradeScoutPro`  
**Audit time context:** 2026-08-04 local / Aug 4–5 UTC commits.  
**Evidence revision:** `origin/main` = `f88fdde7` (merge of PR #270). Working tree = branch `jw-stone/color-swatch-selector` with **uncommitted** follow-ups (no remote branch).

---

## 1) Intake summary

**Outcome today was trying to achieve**

Turn `/jw-stone` into a **catalog-first luxury showroom**: remove customer-path / learning theater; show real inventory by category and color; keep First Cut honest; fix Direct Connect and saved-stones email; collect promo opt-in and customer role on the request form; preserve JW lane isolation; ship only after owner local preview GO.

**Definitely true (from git / PR / locks)**

- PR [#270](https://github.com/infotradescout/tradescoutAI/pull/270) merged to `main` at `f88fdde7` (head `4476ab86`).
- PR [#269](https://github.com/infotradescout/tradescoutAI/pull/269) (JW lane isolation) also merged earlier the same day.
- Owner chat / SI amendment `1.3.0` voids path-guide, Learn-about-stone, CFA / Trending Selection theater; requires header **and footer**, New Arrivals, Aesthetic + Color filters, no merge without GO.

**Assumptions (bounded)**

- “Today” = commits and working-tree work dated 2026-08-04 (local) through the PR #270 merge window.
- Production Auto-Deploy follows `main`; live Render smoke was **not** re-run in this audit.

**Constraints**

- Audit only; no force push / merge / feature fixes.
- Prefer leave this audit file uncommitted.

**Next skill:** objector (below).

---

## 2) What shipped

### On `main` (production path) — PR #270 / `f88fdde7`

| Area | Status on main |
| --- | --- |
| Path-guide / buyer theater removed | **Shipped** (`CustomerPathGuide` gone) |
| Catalog-first page shell (header, hero, First Cut, inventory) | **Shipped** |
| Browse by material (`MaterialCategoryRail`) | **Shipped** |
| Browse by color rail | **Shipped as material-style photo cards** (not compact swatches) |
| First Cut photos, no invented names | **Shipped** (photo slots + shared note) |
| Categorized inventory + filters (aesthetic/color/material URL state) | **Mostly shipped** |
| Stone detail Ask + facts + color palette in dialog | **Shipped** |
| Color swatches on inventory **cards** | **Not on main** |
| Full-slab / never-crop display (`object-contain`) | **Partial** — detail hero uses contain; **cards / First Cut / material covers still `object-cover` on main** |
| Marketplace footer **component** | File exists |
| Marketplace footer **on the page** | **Not mounted** — tests on main assert footer **null** |
| Sticky CTA label “Contact” | **Not on main** — still “Tell JW Stone what you need” / Connect |
| New Arrivals section | **Code exists, not wired** into marketplace; main tests expect no “New Arrivals” copy |
| Trending section | **Code exists, not wired**; main tests assert trending null |
| Express DC email delivery fixes | **Shipped** (recipient fallback, allow-listed purposes) |
| Email my saved stones API + UI | **Shipped** |
| Promo / updates opt-in (default unchecked) | **Shipped** |
| Customer role on materials request | **Shipped** |
| JW lane isolation docs / branch rule | **Shipped** (PR #269) |

### Only local / unpushed — `jw-stone/color-swatch-selector`

- Branch points at `origin/main` (`f88fdde7`); **no** `origin/jw-stone/color-swatch-selector`.
- **Uncommitted** working tree (key deltas vs main):
  - Compact **Browse by color** swatches (`COLOR_SWATCH_OPTIONS`) — not cover-photo cards.
  - **`<MarketplaceFooter />` remounted** after inventory.
  - Sticky band label → **Contact** (drops “Tell JW Stone what you need”).
  - **Never-crop:** `object-contain` on cards, First Cut, material covers, wishlist thumbs, detail thumbs; card palettes restored.
  - Tests updated to require footer + Contact + swatch ids; local Playwright `.last-run.json` shows **passed** (not a substitute for owner visual GO).

---

## 3) Objector findings

Severity: **P0** blocks truth / owner non-negotiable · **P1** material gap · **P2** process / proof debt.

1. **P0 — Footer missing on live page despite owner/SI requirement**  
   Intent lock and evidence claim a real footer. On `main`, `JWStoneMarketplace` does **not** render `MarketplaceFooter`; unit test expects `[data-testid="jw-marketplace-footer"]` **toBeNull**. Component exists unused. Local uncommitted work remounts it — **not shipped**.

2. **P0 — Merge without recorded owner preview GO**  
   PR #270 test plan left unchecked: “Owner local preview GO” / “Do not merge until visual approval.” Merge still landed on `main` (production path). Preview-before-deploy / confirm-intent gate **failed**.

3. **P1 — Contact sticky still wrong on production**  
   Owner ask: Contact label, not “Tell JW…”. Main still pulls `ctaHeading: "Tell JW Stone what you need"`. Fix is local only.

4. **P1 — Never-crop not complete on production**  
   Owner ask: full-slab / never crop. Main inventory cards and First Cut still `object-cover`. Cover ranking helpers exist; display crop remains. Local contain pass unpushed.

5. **P1 — Browse by color still commodity-card style on production**  
   Main color rail uses large stone cover photos (same visual pattern as material cards). Compact swatch grid is local only. Also mislabels aesthetic ids (e.g. bold-expressive → “Green”) on main; local relabels toward Multicolor / Black & dramatic.

6. **P1 — New Arrivals claimed but not on page**  
   SI evidence / decisions say New Arrivals restored. `NewArrivalsSection` is orphaned (only self + unit tests). Marketplace tests on main assert absence of “New Arrivals”. **Claim contradicted by mount graph.**

7. **P1 — Evidence doc overclaims vs shipped UI**  
   `.selective-intelligence/builds/jw-stone-marketplace/evidence.md` lists footer + New Arrivals as implemented while remaining “owner preview.” Shipped `main` UI does not match that ledger.

8. **P2 — Unauthorized / unapproved removals pattern**  
   Learn-about-stone briefly added (`2afd05a1`) then removed (`4476ab86`) under owner void — OK if owner-authorized. Footer **absence on page** and New Arrivals **omit** are not clearly owner-GO’d; tests encoded the omission. Process debt: step-1 intent / APPROVE gate not enforced before merge.

9. **P2 — Production email behavior unproven in this audit**  
   Contract tests cover DC + saved-stones wiring. No live send / Render inbox proof in this pass.

10. **P2 — Color-swatch branch not pushed**  
    Follow-up corrections sit only in local working tree; risk of loss and production stays on defective UX.

**Objector verdict:** `sustained` (blocking truth gaps on footer, Contact, crop, color UX, New Arrivals claim, merge-without-GO).

---

## 4) Verifier scorecard

Legend: **enforced** (locked + true on main) · **done** (on main, matches owner) · **partial** · **missing** · **contradicted** (docs/tests claim X, runtime/main shows ¬X).

| Owner ask | Verdict | Proof note |
| --- | --- | --- |
| Catalog / showroom not commodity dump; categorized inventory | **partial** | Material categories + inventory filters on main; color rail still photo-card commodity look on main |
| Luxury / showroom model | **partial** | Path guide gone; density improved; footer/Contact/crop gaps undercut showroom |
| Full-slab / never crop photos | **partial** → local **done**, main **missing** on cards | Main cards/First Cut `object-cover`; local `object-contain` |
| Color palettes on stones + complements | **partial** | Detail dialog has `StonePalette` on main; **cards** only in local work |
| Browse by color = compact swatches | **missing** on main | Local swatch grid unpushed |
| Material categories preserved (Browse by material) | **done** on main | `MaterialCategoryRail` mounted; local also never-crops covers |
| First Cut photos; no invented names | **done** | Photo slots + section note; empty assignments |
| Detail Ask + facts | **done** on main | Ask JW + material/availability/finish/dimensions (+ local origin) |
| Footer must exist | **contradicted** on main | Required by lock; unmounted; tests expect null; remount local only |
| Contact sticky (not “Tell JW…”) | **missing** on main | Local Contact label only |
| DC emails fixed | **done** (code) / **unproven live** | Recipient fallback + purpose allow-list + contract tests |
| Email my saved stones | **done** (code) / **unproven live** | Route + WishlistPanel + contract tests |
| Promo opt-in | **done** (code) | Default unchecked; `updatesOptIn` persisted when true |
| No unauthorized removals | **contradicted / process fail** | Footer/New Arrivals omissions; merge without GO |
| Confirm-intent / preview GO | **missing / failed** | PR #270 unchecked GO items; merged anyway |

**Overall verifier status:** `partial` (backend/email + catalog skeleton on main; several owner-critical UI asks only in unpushed local work; process gate broken).

**Aligner status:** `blocked` for calling the day “complete.” Approve resume only after owner preview of the color-swatch working tree (or a committed PR) and production smoke of email paths.

---

## 5) Aligned handoff — what owner should do next

1. **Local preview (required)** — Run app, open `/jw-stone` with the **current working tree** (`jw-stone/color-swatch-selector` uncommitted fixes): desktop + mobile. Confirm Contact, footer, compact color swatches, never-crop, material rail, First Cut honesty.
2. **Decide New Arrivals** — Either mount `NewArrivalsSection` when items exist, or explicitly void the SI “New Arrivals required” line so locks match reality.
3. **Commit + push** color-swatch branch from `origin/main` only after your visual GO; open a JW-scoped PR. Do not merge without that GO.
4. **Production verify emails** after deploy of the next approved slice: Express DC notify to JW inbox, requester confirmation, “Email my saved stones,” marketing opt-in storage (unchecked by default).
5. **Process debt** — Treat merge of #270 without checked preview GO as a real SI/control failure; do not repeat. Keep step-1 intent / APPROVE before main.

---

## 6) Resume packet

```
project: JW Stone marketplace
date: 2026-08-04
main: f88fdde7 (PR #270)
local_branch: jw-stone/color-swatch-selector (uncommitted; no remote)
si_final_status: partial
alignment: blocked
blocking: footer/Contact/never-crop/compact-swatches not on main; New Arrivals claim false; merge-without-GO
next_safe_step: owner local preview of working tree → commit/push color-swatch PR → GO → merge → prod email smoke
```

---

## Evidence index (short)

- `git log origin/main` (2026-08-04): `57963839` #269 lane isolation; `2afd05a1` / `4476ab86` / `f88fdde7` #270.
- Main marketplace omits footer mount; test expects footer null + “Tell JW Stone what you need”.
- Local diff remounts footer, Contact label, compact swatches, `object-contain` across media surfaces.
- `NewArrivalsSection` / `TrendingSection` unused by marketplace page imports.
- Server: `server/routes/jw-stone-saved-stones-email.ts`, `tradepartner-express.ts` opt-in + email fallback; contract tests present.
- SI locks: `.selective-intelligence/intent-contract.md` amendment `1.3.0`; evidence still says preview required.
