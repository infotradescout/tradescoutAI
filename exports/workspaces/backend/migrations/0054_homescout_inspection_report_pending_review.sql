-- HomeScout inspections: allow a pending_review status for community contributed (buyer) uploads.
-- The DB uses varchar for status, so this is mostly a documentation marker migration.
-- Safe no-op: it does not change schema in a breaking way.

-- Intentionally empty.

