// Shared profile-related constants
// Bump this when making a breaking change to profile/onboarding semantics
// that should trigger a one-time profile reset/normalization.
export const CURRENT_PROFILE_VERSION = 1;
