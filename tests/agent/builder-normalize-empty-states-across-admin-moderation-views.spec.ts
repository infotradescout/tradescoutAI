// builder placeholder test (skipped)
// TODO(agent): replace with real task later
import { test } from "@playwright/test";

test.skip("builder placeholder: builder-normalize-empty-states-across-admin-moderation-views", async () => {
  // no-op
});
