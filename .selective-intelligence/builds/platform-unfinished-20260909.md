# Recovery and action-first integration checkpoint

Created 2026-09-09 UTC. Worker lane: platform_integration. Branch: codex/platform-unfinished-integration-20260909. Base: ec1079fd1d68f170cbe15209c496624884b1ef4b. Incoming onboarding: 0b8734857bc0abb8c9e0ab1d445fb3d09e7e3470.

The user asked to finish interrupted work and authorized continued recovery and action-first onboarding. This slice combines those existing drafts into one local candidate that saves a recommendation before full setup, preserves its content through authentication, and holds publication until current verification and moderation approval. It preserves the recovered county request/contact, identity, staff messaging, and migration behavior. Root owns final integration review, push and draft PR; no main merge, deployment, production database writes or external sends are in this Worker scope.

Canonical owners are the existing recommendation repository, route handlers, public recommendation serializer, shared account continuation, migration journal, required-schema verifier and current profile components. Resolve overlaps by semantic ownership; never choose an entire conflicting version for convenience. Preserve the published main migration prefix byte-for-byte. Restore recommendation columns before applying the publication projection. Any unshipped migration rename must preserve its source SQL and be documented with both identities.

Protected invariants: numeric legacy ratings do not imply sentiment, endorsement, moderation or publication. Unknown sentiment remains private and unclassified. Publication requires an explicit positive/negative type, public flag, approved moderation and current author verification. Verification and moderation changes repair projections atomically. Submission does not grant contact, account privileges, county authority or exposure; all existing gates remain.

Proof sequence: resolve conflicts; run focused recommendation, schema, identity, contact and affected UI suites; run disposable native PostgreSQL migration, legacy and concurrency cases; render and use current built desktop/mobile recommendation and recovery journeys; commit a clean candidate; run its strict minimum release contract. Use only synthetic accounts on a loopback test database with real providers disabled. Stop only processes and database instances owned by this lane. Keep baseline bloat limits intact and report known/unexecuted proof precisely.

The checked-in global SI lock describes JW Stone 2.0 and has 14 pre-existing digest errors; it is not this platform slice's authority and is preserved. The existing platform recovery contract and this subordinate checkpoint/execution contract define the active lane. Index findings are recorded without claiming an all-clear inventory. Other portfolio work, Infinity refresh, Exchange connection, external-provider proof and release remain with their named owners.

## Integration decisions and initial evidence

Main 60dd7379266bd22f2304a9b2833fad3e7d375759 has 139 journal entries. Each entry and its normalized SQL bytes were compared with this candidate and are unchanged. Recovery entries 0136 notification (index 139) and 0137 recommendation restoration (index 140) precede onboarding's publication projection. The latter was renamed from unshipped 0136_recommendation_publication_projection.sql at onboarding commit 0b873485 to 0138_recommendation_publication_projection.sql (index 141, original timestamp 1788918000000). Its SQL bytes are identical, including the historical tradescout-schema:0136:v1 trigger marker; required-schema verification deliberately checks that original marker. The ledger, function-body checks, tests and current filename agree. Neither draft was merged to main by this work.

The onboarding repository owns recommendation writes, moderation, public reads and projection updates. Both sets of guards remain: explicit canonical type, private defaults, immutable legacy sentiment, current author verification, approved moderation, and current author email matching. A semantic integration gap in Trust/CVS scoring was repaired: revoked or changed author email now removes recommendation credit just as public reads and cached counts do. Native negative tests exercise actual extracted route/storage owners using rollback-only fixtures and savepoints.

Observed before final combined checkpoint: 65 focused schema/recommendation tests, 132 affected UI/authority tests, 35 native PostgreSQL recommendation cases, fresh migration and independent readiness verification, legacy restoration/replay/privacy/scoring cases, and recovery schema adversarial cases passed. One recovered notification fixture expected a null sent_at after preference suppression; the existing outbox implementation intentionally records dispatch completion separately from delivery. The test now requires dispatch completion and zero delivery records, preserving the no-send invariant without replaying suppressed notifications.

For final verification, Infinity's separately reviewed refresh 6dd00a906597524e5b767961c6f2fd0248e1f545 is an authorized dependent input. The generated index is refreshed after integration. Prior branch attestations remain historical; final native/browser/build/release-gate evidence must identify the combined source. Production release, external email/provider exercises, Docker proof and existing file-count budget failures are not closed by local evidence.

The expanded native notification proof requests email with notifications disabled and replaces the provider adapter with a counter that fails if called. The current canonical producer persists requested email intent transactionally; the worker then records terminal cancelled/notification_ineligible state with zero success, no retry, and null sent/delivered/provider receipt. The native proof verifies that state, zero adapter calls on the first and repeated worker pass, and zero delivery rows for the separate suppressed in-app notification. This distinguishes a cancellation audit record from a sent or delivered email and does not weaken the no-consent/no-external-submission invariant.

Independent Objector review reproduced one remaining Trust/CVS mismatch: case-folding moderation status admitted legacy `APPROVED` and `Approved` rows that the canonical public read and publication projection reject. Scoring now requires exact `approved` status. The rollback-only native regression first failed with four positive signals instead of two, then passed after the predicate repair; uppercase, mixed-case and whitespace statuses earn no Trust/CVS, public-read or cached projection credit. Exact-commit release and browser receipts for the corrected candidate supersede the earlier successful 43908b29 checkpoint.

The same review reproduced a stale owner-facing status on same-ID submission replay after an optional legacy account `is_active` flag was disabled: POST omitted that flag and reported publication while mine/public/projection withdrew it. The submit response now selects the repository's shared current author verification fields. The real native route fixture failed before the fix and verifies replay/mine response equality, private/unverified status and unchanged withdrawn public totals afterward. This changes neither authentication policy nor schema.
