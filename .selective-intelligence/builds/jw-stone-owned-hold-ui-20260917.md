# JW Stone existing-reservation recovery UI

Objective: Expose existing owned reservation identity, quantities and deadline in the real site without requiring current member pricing or enhancement access. Keep production ON unless Thomas explicitly says OFF. Reserve/release mutations and idle-worker startup remain a separate unfinished boundary.
Base branch/commit: jw-stone/feature-access-control-20260917 at ee72caae7f624de1fc846ff448a63082373ff5c8.
Current branch/commit: Same branch; resolve this checkpoint's enclosing commit. Workspace ~/scout-verification-20260916/jw-feature-integration on TSCommandCenter.
Verified completed work: Full application TypeScript and affected existing recovery, pricing-route and contact-delivery tests passed. See ../evidence/jw-owned-hold-ui-20260917/{typecheck.log,focused.log} for actual counts. No new DOM unit tests are claimed.
Implemented: Extracted only the existing active/request-key recovery GET handlers into an idempotent registrar, mounted with actual authentication and existing stone-schema guard by the member-pricing registrar. The original future full hold registrar reuses that extraction. Private cache headers precede authentication on these reads.
Implemented UI: JwStoneReservationStatus in the existing marketplace displays owned reservation ID, slab quantities, original deadline and server-reported state. It refreshes on focus/every 30 seconds, preserves ordinary contact and uses the canonical viewer-bound price-free response parser. No local/session-storage writes, new reserve/release/renewal request or entitlement override is introduced. No current hold or initially unavailable endpoint leaves the base catalog alone; failed refresh labels last-confirmed data.
Database effect boundary: Existing recover() may expire a due owned hold through its existing guarded ledger transaction. GET recovery does not create or extend a reservation or bypass ownership. No new worker or automatic feature-state change is introduced.
Changed but unverified work: New positive native browser coverage for existing-hold recovery/reload and visibility while OFF/revoked is prepared but has not yet executed. It uses the existing loopback signup/application harness and separately invokes the existing ledger service to create/clean up synthetic fixture holds; this is not a browser reserve/release journey.
Files changed: client JwStoneReservationStatus.tsx and JWStoneMarketplace.tsx; server jw-stone-cart-holds.ts and jw-stone-member-pricing.ts; scripts jw-stone-owned-hold.fixture.ts, jw-stone-offer.native.mjs, jw-stone-feature-journey.mjs; this checkpoint and pointer.
Tests/evidence already run: npm run check; npm run test:run -- server/tests/jw-stone-cart-hold-recovery.test.ts server/tests/jw-stone-member-pricing-route.behavior.test.ts server/tests/express-request-contact-delivery.behavior.test.ts --maxWorkers=2. Previous 29 ledger/native and controller release results are historical, not new evidence for this UI.
Tests/evidence invalidated by later changes: The application route/UI additions require a new build and real browser acceptance. No current full release-gate pass is claimed. Existing pricing/offer/controller evidence is not certification of a complete reservation UI.
Known blockers/risks: The requested mutation-runtime append and new reserve retry-operation helper write were rejected by the tool safety check. The partial untracked runtime file was verified and removed; no alternative implementation of the rejected rate/mutation/helper operation was attempted. A new DOM-test append was rejected; its incomplete untracked fixture was removed and no cases from it are counted or reconstructed. Continued only the independent existing-record recovery GET/UI slice.
External side effects and retry safety: Local source changes and isolated tests only. No production setting, data, stock, payment, account, membership, message, main merge or deployment. Earlier commercial workspace/process uncertainty remains untouched. Recovery remains owner-scoped and does not repeat an uncertain reserve command.
Next exact action: Commit the current scoped slice, run the existing native driver with --feature-control --owned-hold-status in the isolated toolchain with fresh preflight, inspect its exact final receipt and screenshots, fix only demonstrated issues and publish. Preserve the separate rejected runtime/operation-helper boundary rather than claiming reserve/release/countdown or worker startup exists.
Actions that must NOT be repeated: No production OFF test or billing suspension, no old commercial recovery/reconstruction, no broad pricing/Scout audit or account-harness rebuild, no source-price inventions, no security-check bypass, no full reservation-release claim from service-created fixtures.
Acceptance target for this slice: Signed-in original owners see the same existing reservation after reload and while enhancements or membership are unavailable; base contact is unaffected and no price information is returned. Full requested reservation controls remain incomplete until permitted mutation wiring and actual reserve/release/expiry browser proof exist.
