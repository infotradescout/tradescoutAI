# Exchange profile inventory recovery

Base: 60dd7379266bd22f2304a9b2833fad3e7d375759. Branch: codex/exchange-profile-items-recovery-20260909. Worker: /root/exchange_recovery. Root owns the current intent lock; the older repository-wide JW visualizer lock is not this platform slice.

The user's correction requires profile inventory to also appear on Exchange while the public profile remains intact. This slice makes published, discovery-authorized inventory individually findable with its existing images, honest material identity, exact profile item destination, and existing protected request journey. Native item offers and ordinary listings remain available without duplicated records. Search, explicit geography filters, deterministic sorting and pagination apply to the composed result.

Canonical owners are extended: profileCatalogExchange for request-only discovery; profileItemShareMetadata/profileItemShare/profilePublicItemRoute for inventory, naming and exact destinations; ProfileRepository and ownerConfirmedDirectProfile for public discovery eligibility; routes.ts for Exchange composition; the existing Exchange pages for rendering. The competing Exchange list interception in public-profile-app is removed. There is no second JW inventory owner, inventory write, email-verification change, new purchase route, or new contact authority.

Authority decision, aligned by root: read-only profile inventory uses canDiscoverPublishedProfilePublicly (mode public), which requires published release, business binding, current moderation/trust, discovery permission, and relevant professional approval. Sale offers and marketplace rows retain hasExposureAuthority. Published alone, private/draft, suspended/unapproved, mismatched owners, direct-only, and unlisted-review profiles are insufficient. An item view grants no contact or purchase permission.

Read-only source evidence at 2026-09-09T05:01:03Z: JW and ISSA both resolve to canonical public discovery; JW does not pass the separate email prerequisite for sale exposure. No owner records changed. Current Exchange Building Materials API shows ISSA catalog alone. Live release remains unverified until a separately authorized merge and deployment.

Reuse gate: refreshed project index has 11 baseline ownership/duplicate findings outside this slice (PostCSS config pair, onboarding aliases, procurement re-exports, AppShell, preserved theme aliases, Home/landing, JW inventory section alias and wholesaler legacy export). They are bounded compatibility/source-owner exceptions for this repair; their files are unchanged. Existing inventory/route/gating owners are reused. Index freshness and new findings will be checked after edits.

Proof required: behavior tests for canonical projection, identity/images/material, discovery authority, strict geography/price/condition/search, native source retention, overlapping IDs and routes, and pages beyond 100; typecheck/build and affected law/contact contracts; rendered desktop/mobile discovery to exact item to protected request entry without submitting. Synthetic fixture evidence is labeled; live state evidence is read-only. No merge/deploy/send authority is exercised by this worker.

Root-approved scope corrections found during rendered review: the activity-based ProgressiveFeatureGate redirected a fresh guest from Exchange to Scout, and the shared evaluateFeatureUnlocks owner hid Exchange from the mobile Menu. Public Exchange browse/category/detail routes and their navigation now work with zero qualifying activity. Other advanced feature unlocks and protected seller/create/purchase routes retain their existing gates. Main Exchange now trusts the server's business/material search matches, offers reachable 48-item pages, and omits unproven category totals. Shared mobile styling was overriding the item action's background; the existing Button uses explicit accent/background text utilities for a readable action.

Independent Objector review found a nullable-date ordering mismatch in the bounded native source window. Public-only repository reads and the native offer query now use NULLS LAST, matching the compositor; other repository consumers retain prior behavior. A real Drizzle/PGlite test runs the actual marketplace repository with 120 dated rows, 120 undated rows and 110 ineligible rows, and compares all four date/price page traversals against an independent PostgreSQL UNION/order oracle. IDs remain unique and every eligible row is reachable. Runtime sale authority remains unchanged and separately matches SQL across current verification branches.

Rendered evidence: tmp/exchange-proof/browser-proof.json and desktop/mobile screenshots. The actual production client build runs against a loopback read-only API fixture captured from the current adapter and actual public profile content. Custom-domain navigation is local and viewer state is synthetic anonymous. The contactPolicy literal matches server/routes/profiles.ts; this proof establishes visible navigation only, not submission authorization. Mobile follows fresh Scout -> Menu -> Exchange -> Building Materials with no seeded or qualifying activity. Both sizes traverse 48 -> 96 -> 122 current captured items; JW Stone search traverses 48 -> 96 -> 120; granite search returns 23. Those are observations, not acceptance constants. Exact Blue Dunes route opens its existing photos/material, then the existing request form retains Blue Dunes and exposes contact consent and the submit control without submitting. Seller dashboard still routes to sign-in. No page errors or horizontal overflow; item action contrast is 7.49:1 on both sizes.

Evidence boundaries: the catalog adapter's configured-database audit uses a READ ONLY transaction; the browser fixture, actual route-composition behavior, and disposable PostgreSQL tests are separate proofs. They are not full Express/native production-database end-to-end or live deployment proof. Production writes, customer/provider sends, merge, and deployment are not performed. Full minimum-release attestation with disposable full-schema migrations, authenticated commerce submission, external-provider behavior, and post-deployment smoke remain for the separately authorized release lane.

Local verification at the reviewed source: 89 tests across 14 affected suites pass, including actual PostgreSQL policy parity and nullable pagination; npm run check and npm run build pass; guard:law-drift, guard:forbidden, audit:authority-gates and audit:trust-leaks pass. Evidence logs are under tmp/exchange-proof. Index refreshed after implementation; doctor retains exactly the 11 baseline duplicate/compatibility-owner errors and five primitive warnings listed above, with no stale or missing-owner finding. Independent Objector reviewed the exact authority, paging, search, and fresh-guest navigation changes and resolved both source-order and navigation objections before the local checkpoint. guard:bloat requires a clean commit and is checked separately after that checkpoint.
