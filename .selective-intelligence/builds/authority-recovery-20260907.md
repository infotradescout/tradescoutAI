# Canonical authority recovery

The user's outcome remains finishing outstanding TradeScout work while preserving existing worktrees and later portfolio deliverables. This active slice closes the role-guard seam in PRs #570 and #572 against recovery checkpoint `6ee9d11d3a12759fee822ea0c6f14a4622a926f6`, in `D:/ts-authority-recovery-20260907` on `integration/authority-recovery-20260907`.

The source stack #568 → #569 → #570 → #571 → #572 is not a runtime dependency requirement. #568 dormant copy/adapter cleanup, #569 component and JW-specific work, and #571 OAuth provider-subject/account-linking policy remain separate. This slice does not replay their product changes or restore old email-alias privilege. Existing fresh effective-account binding, impersonation boundaries, professional approval, contact receipts, county routing, and current profile/JW behavior remain authoritative.

Canonical owners: `shared/roles.ts` supplies the explicit role-grant predicate; `server/auth.ts` binds and guards request authority; compatibility guards and router gates consume it; `RoleProtectedRoute.tsx` uses the same explicit role meaning. Passport deserialization loads the normal principal from storage; the request authority resolver refreshes an impersonation target and restores it if intervening middleware replaces `req.user`. Numeric ranking remains available for actual role-assignment/display operations. Existing named allowlists remain unchanged after a separate consumer review.

Acceptance: a generic persisted admin flag does not become super-admin; explicit persisted super-admin role/flag remains accepted; unrelated ranked roles are denied; each boundary's named roles remain accepted; unauthenticated, stale-session, invalid-identity and impersonated privileged actions fail closed; reserved email aliases never grant power; aliases are the same guard instances; client loading/auth/role states match the explicit predicate. Focused authority/identity/professional tests, type/build, and authority audit provide local evidence. Native DB/provider/browser/release proof will be named honestly if unexecuted.

The root agent authorizes this isolated implementation, clean local npm install and a reviewable staged/committed candidate. No push, main merge, deployment or database migration is included. The frozen prior recovery candidate is not edited.

Source-backed dependency disposition:

- #568 (`f9837e71`) removes dormant Infinity inheritance adapters and copy. No changed guard imports those adapters, so that cleanup is separate.
- #569 (`53edb56b`) converges UI owners and contains JW product work. Neither the platform UI cleanup nor JW lane is replayed here.
- #570 (`c6c2f372`) supplies canonical guard convergence and the generic-admin/super-admin distinction. Its email-alias privilege is intentionally excluded because the recovered branch already removed that authority source.
- #571 (`42b16b9a`) changes OAuth provider subject matching and account linking. It is a distinct identity seam and remains unchanged here; this candidate does not prove social account association safe.
- #572 (`57bb4aa4`) supplies explicit named role grants. It needs the role helper and canonical role gate, not the OAuth or product changes preceding it in Git history.

The rendered `RoleProtectedRoute` primitive currently has no production consumer in `client/src`. Its tests prove the corrected primitive behavior only. The separately used `ProtectedRoute.tsx` and `useRoleAccess` retain their existing global admin UI bypass; `requirePermission` retains primary-role permission semantics; business and record membership models remain separate work. This candidate does not claim that all client visibility or every authority boundary is reconciled.

Law classification for this slice: named role entry and privileged impersonation rejection are **enforced** by the modified canonical guards and their tests. Existing visibility/access separation, contact intent/decision gates, county context and professional approval have no implementation changes here; focused Direct Connect and professional suites cover their existing guard contracts, not every customer journey. Converging all independent membership, client visibility and permission sources is a **policy_target**. There is no new temporary exception.

Verification in this isolated checkout:

- `npm ci`: passed with a local install, 1,305 packages; the unchanged lockfile reports one moderate and one high advisory. No automatic dependency upgrades were applied.
- `npm run test:run -- server/tests/canonical-authority-guards.test.ts server/tests/identity-authority-spine.contract.test.ts server/tests/request-authority-context.test.ts server/tests/request-authority-middleware.behavior.test.ts server/tests/privileged-email-authority.regression.test.ts client/src/components/auth/RoleProtectedRoute.test.tsx --reporter=dot`: 6 files, 86 tests passed, no skips. Database environment variables were empty.
- `npm run audit:authority-gates` and `node scripts/guard-identity-authority-spine.mjs`: passed. The spine guard now verifies exact canonical aliases/imports instead of requiring duplicate implementations; behavior tests execute those same entry points.
- `npm run test:run -- server/tests/professional-role-authority.contract.test.ts server/tests/professional-application-storage.test.ts server/tests/professional-route-extraction.test.ts server/tests/phase2c-token-impersonation.router.test.ts server/tests/admin-substring-authority.contract.test.ts server/tests/direct-connect-gates.regression.test.ts server/utils/authorityPolicy.test.ts --reporter=dot`: the six unchanged suites passed 114 tests. Four token-router tests initially failed because their test app supplied `req.user` without Passport's `isAuthenticated` method. The test fixture now supplies that production method; exact canonical denial, token and audit assertions remain. A focused rerun of `server/tests/phase2c-token-impersonation.router.test.ts` passed all 6 tests, including added anonymous and generic-admin denials. Aggregate final proof: 13 files, 206 tests passed, no skips.
- `npm run check`, `npm run build`, `npm run guard:law-drift` and `git diff --check`: passed. Existing Tailwind ambiguity and large-chunk build warnings remain; all bundle/media/deduplication verifiers passed. The build's line-ending-only sitemap changes were restored from this lane's original HEAD.
- Actual full `createApp` HTTP smoke via `node --import tsx tmp/authority-app-smoke.ts`, using only the authorized disposable loopback database and existing synthetic accounts: real Passport login and session cookies; anonymous 401, ordinary account 403, super-admin 200 at both `/api/admin-control/state` and `/api/prompt-admin/status`. Social providers and schedulers were disabled; email was unconfigured. No prompt or control writes. The process exited 0 and port 5201 was released. The retained harness is `D:/ts-authority-recovery-20260907-http-smoke.ts` and execution log is `D:/ts-authority-recovery-20260907-http-smoke.log`.
- Source index refreshed against the final implementation. Its existing 16 errors and 5 warnings match the initial checkout; this slice adds no runtime owner. `npm run guard:bloat` first correctly refused the uncommitted checkout, then failed four inherited budget categories on the clean candidate: base `6ee9d11d` tracked files 4,054 → 4,057 (limit 3,950), unique blobs 4,033 → 4,036 (3,900), Docker files 3,225 → 3,227 (3,125), and unchanged `server/storage.ts` 285,247 bytes (284,246). The added paths are the two focused test files and this evidence artifact. All byte/repetition/public-file budgets, Docker exclusions and other monolith ceilings pass. Budget policy and unrelated source were not changed; this remains a release verification failure for the root's separate bloat reconciliation lane.
- Root's independent source objector found no blocker in explicit grants, canonical aliases or standalone routers; it independently verified that existing server role allowlists already name the intended roles. The requested full-app smoke above closes the mocked-import limitation for these routes.

Exact changed source and proof files: `server/auth.ts`, `server/middleware/requireSuperAdmin.ts`, `server/routes.ts`, `server/routes/admin-control.ts`, `server/routes/promptAdmin.ts`, `shared/roles.ts`, `client/src/components/auth/RoleProtectedRoute.tsx`, `client/src/components/auth/RoleProtectedRoute.test.tsx`, `server/tests/canonical-authority-guards.test.ts`, `server/tests/identity-authority-spine.contract.test.ts`, `server/tests/phase2c-token-impersonation.router.test.ts`, `scripts/guard-identity-authority-spine.mjs`, `.selective-intelligence/project-index.json`, and this artifact.

No migration, provider callback, deployed browser, main merge or production verification has run for this new authority slice. The native smoke verifies only these two read-only administrative endpoints and synthetic local session flow.
