# Build Evidence: bidrock-marketplace-recovery

Status: auction-first correction and isolated database proof passed; temporary preview redeploy and browser proof remain pending
Started at: `2026-08-20T18:58:08Z`
Base revision: `5e6c44a49fcb0eae0cf720fadd898c477d8d8293`
Branch: `jw-stone/bidrock-marketplace-recovery`

## Confirmed inputs

- Seven physical lots and their exact dimensions/quantities are owner-approved in the Worker checkpoint.
- The refreshed project index is bound to the base revision above.
- Historical source branches are reuse evidence only; their giant inline BidRock page and catalog-as-active-listing assumption are rejected.
- Gold Macaubas is owner-confirmed as 6 slabs with 2 polished slabs. This current owner statement supersedes the older 1.5-polished filename evidence while preserving that historical source record.
- The product-owner `REPLACE` amendment approved at `2026-08-20T19:23:32Z` removes the prior BidRock revenue decision. BidRock adds no marketplace fee and canonical fee fields must be zero.

## Governance correction checkpoint

- The Aligner marked the release blocked and authorized only the bounded correction pass recorded in the active build contract.
- The active build must remain `in_progress` until schema migration, authorization, concurrency, canonical-link, handoff, client-capability, and proportional proof findings are reconciled.
- Historical JW marketplace and 3D build records remain preserved; their evidence is not rewritten as proof for this BidRock correction.

## Auction-first correction evidence (`2026-08-21`)

- This section records the approved auction amendment and supersedes the older visible offer/catalog workspace claims retained below as historical continuity evidence. Historical offers and orders remain readable; no offer, counteroffer, fixed-price, or Buy It Now action is routed as the primary auction experience.
- `shared/bidrock.ts` now owns the timed-auction DTOs, states, reserve/bidder states, money shape, and the exact 120-second soft-close constant. Guest/unverified shaping omits opening, current, minimum-next, reserve-amount, and maximum dollar fields; verified bidders receive public auction dollars plus only their own maximum/status; seller/admin configuration includes the reserve amount but never another bidder's maximum.
- Ordered migration `0119_bidrock_timed_auctions.sql` adds durable `bra_*` auction and immutable maximum-bid identities, a per-listing current-auction uniqueness guard, positive/coherent time/value checks, accepted-sequence ordering, idempotent bidder keys, immutable bid/origin triggers, exactly-one offer-or-auction order origins, and unique close outcome links. It is journaled after `0118`, explicitly reconciles reservation/order auction foreign keys, and passed two consecutive applications on the isolated preview branch.
- `server/services/bidrockService.ts` uses locked transactions and current database wall-clock readings for maximum bids and closure. Proxy price keeps a sole leader at opening until its private maximum meets reserve, then exposes reserve as the effective price; it uses only each bidder's latest maximum, advances against the runner-up by the increment, and favors the earlier accepted sequence for an exact maximum tie. Seller owners, delegated seller agents, and administrative accounts cannot bid. A bid in the final two minutes adds exactly two minutes to the existing end using one materialized database timestamp.
- Seller/admin auction configuration is an explicit mutation and requires current published physical stock, exact publication authority, positive opening/reserve/increment values, coherent times within the stock confirmation window, and pickup/freight terms. Catalog and auction GET routes remain read-only. Active/current auctions block stock editing, retirement, unpublication, and legacy offer/counter/accept paths.
- Expired-auction maintenance is an explicit admin mutation. A reserve-unmet close records no sale and creates no commercial chain. A reserve-met close atomically allocates the whole physical lot and creates exactly one auction-origin reservation, allocation, and order on the existing ACH-only logistics path. No fee field is introduced, and canonical zero-fee validation remains unchanged.
- The routed `/bidrock` feature is now an auction-house front with large factual imagery, lot number, dimensions, slab count/finish, countdown, bid activity, reserve state, pickup/freight terms, search/filter/save/compare, seller controls, result states, and a context-preserving detail/bid panel. Guests receive an explicit business-verification message instead of dollar values. Verified bidders can submit a private maximum; recoverable failures preserve the typed amount. The mobile helper accurately says “Enter maximum bid,” while only form submission says “Place bid.”
- The isolated preview database contains exactly the seven owner-confirmed JW lots as five live and two scheduled auctions. Seeded bidder identities and activity use `preview.invalid` test identities, and every pickup/freight term explicitly says “Temporary preview — test activity only.” No production database or payment provider was touched.

### Auction-first local validation

- `./node_modules/.bin/vitest run server/tests/bidrock-*.contract.test.ts client/src/features/bidrock/BidRockWorkspace.test.tsx client/src/features/bidrock/BidRockWorkspace.auth-cache.test.tsx client/src/features/bidrock/BidRockOperationsPanels.test.tsx server/tests/jw-stone-confirmed-stock.contract.test.ts` — passed after the last source edit: 10 files and 65 tests passed. Coverage includes proxy/reserve pricing, same-bidder raise, exact-max tie, owned/delegated seller rejection, transactional identity locks, guest redaction, viewer-scoped cache transitions, additive two-minute extension, reserve-met/no-sale outcomes, exactly-once close/order source contracts, zero-fee/ACH continuity, auction/stock cross-authority guards, auction-first first screen, countdown/activity, primary bid submission, typed-maximum recovery, and safe idempotent preview setup repeats that cannot mutate a lot owned by a current auction.
- Focused Prettier check passed for all changed TypeScript/TSX/CSS and migration-journal files. The repository's installed Prettier has no SQL parser, so migration SQL is covered by focused structural contracts and awaits isolated database execution.
- Focused ESLint exited 0. Its only source warnings were nine pre-existing non-null assertions in `BidRockOperationsPanels.test.tsx` and the existing request-user `any` in `server/routes/bidrock.ts`; server contract tests are excluded by the repository lint configuration.
- Whole-repository `./node_modules/.bin/tsc --noEmit --pretty false` exited 2 only in the pre-existing unrelated admin, HomeID, and managed-partner files listed later in this document; it reported no BidRock or Stone inventory diagnostic.
- `npm run build` passed during this pass: 4,069 Vite modules, public-landing and built-URL verification, and the server bundle completed. Existing Browserslist, Tailwind ambiguity, dynamic-import, and chunk-size warnings remained. Generated fallback images were removed and the sitemap was restored after the build.
- `git diff --check` passed. Routed auction UI searches returned no offer/counter/Buy It Now/private-price helper, no hardcoded numeric dollar literal, and no marketplace fee/commission/take-rate term. Generic canonical fee fields remain only in the existing explicit-zero validation path.
- The project index refresh completed with 212 directories, 3,669 source files, 20,078 symbols, 1,499 components, and 11,171 functions/hooks. Doctor reports `stale: false`; its exit 1 consists of 1,108 pre-existing whole-repository duplicate/competing-owner errors and five raw-element warnings, not a clean whole-repository result.

### Isolated auction database proof (`2026-08-21`)

- Proof used only Neon project `mute-rice-47282135`, temporary branch `br-summer-thunder-ad2iqhct`, database `neondb`. Migration `0119` parsed into 63 ordered statements and completed twice without error.
- Catalog inspection matched all 12 required auction unique-index shapes, including the exact partial current-auction predicate; all 12 auction foreign keys matched their source and target columns with validated `ON DELETE RESTRICT`; both legacy `accepted_offer_id` origin columns allow NULL.
- Two concurrent exact-maximum inserts received accepted sequences 8 and 9; the earlier sequence ranked first. A later maximum from the first bidder became that bidder's sole participating maximum. A conflicting reuse of one bidder idempotency key was rejected and left exactly one preserved row.
- A database-boundary soft-close scenario started with 119 seconds remaining, entered `extended`, and produced an exact 120-second `ends_at - original_ends_at` extension. The visible auction schedule was restored afterward; the accepted row remains clearly labeled preview test activity.
- The preview state contains seven auctions, seven JW listings, and only isolated test bidder/activity identities. Production remains untouched. Database closure/conversion is covered by row-lock/source contracts and unique schema guards; browser and deployed endpoint proof remain pending.

## Implementation evidence

- `shared/stoneInventory.ts` contains the canonical seven-lot physical-stock fixture: 7 lots, 82 slabs, exact owner-confirmed dimensions and quantities, and 2 known polished Gold Macaubas slabs. The broader generated JW material library remains unchanged at 127 photo-backed records.
- `server/services/jwStoneConfirmedStock.ts` projects stable material/passport identities and creates private Stone Core inventory positions under an advisory-locked transaction. Existing positions are not reset on repeat runs. `scripts/backfill-jw-stone-confirmed-stock.ts` defaults to dry-run and requires both `--apply` and an explicit production guard before any production write.
- `server/services/stoneInventoryService.ts` and `server/routes/stone-inventory.ts` provide freshness-aware public current stock plus gated seller/admin CRUD and explicit publication. Every non-admin Stone mutation now requires both an active verified BidRock entitlement and the exact holder-business write or publish capability. Suspended/revoked actors receive no mutation off-ramp. Public responses contain only sale-ready, fresh physical stock and omit seller-only passport, position, source, and location references.
- The actual JW renderer now places seller-published Current Inventory before the separate 127-record Material Library. JW public metadata, discovery summaries, SSR, story copy, and migration `0115_jw_stone_inventory_truth.sql` preserve the library without calling it current stock or advertising public prices.
- The profile-native account dialog creates or reuses the canonical private identity and private business record from the actual JW header and BidRock. It has no role picker, second password, or forced general onboarding. Eligible stone-profile relationships receive the same downstream BidRock entitlement, gated by business verification.
- `shared/bidrock.ts`, `server/services/bidrockService.ts`, and `server/routes/bidrock.ts` implement physical-position projection; fresh, seller-published buyer listings; verified-business private prices; seller ownership checks; saved selections; offer/counter/accept/reject; 48-hour reservation; order; ACH readiness; canonical marketplace/procurement reconciliation; settlement; cancellation; idempotent freight/custody/fabrication/installation-HomeID handoffs; and admin completion with Stone Core quantity release.
- `/bidrock` is a lazy routed React workspace composed from canonical primitives. It provides search, material/source/saved filters, compact lot rows, detail, three-lot compare, guest or account saves, seller inventory and private price/publication controls, and verified-business transaction state. It is a distinct Powered by TradeScout surface rather than an inline landing page or planner.
- Source inspection and focused authorization tests show that public/unverified BidRock DTOs omit private price values and seller-internal position/business identifiers, and that seller mutation helpers require an active verified-business entitlement plus exact ownership/delegation scope. These are local code/test claims, not deployed or browser-observed claims. Contact and Direct Connect gates are unchanged.
- BidRock uses ACH as its only order method and links to existing canonical marketplace/procurement records. The canonical payment service was not modified or directly invoked.
- Migration source and journal contracts for `0116_stone_core_schema.sql`, `0117_profile_accounts_and_entitlements.sql`, and `0118_bidrock_marketplace.sql` declare the new Stone Core, profile-account, entitlement, and BidRock schema, including audit/deduplication before uniqueness and immutable one-to-one canonical links. Runtime services contain no schema DDL, and public GET paths do not project inventory or expire holds. The ordered migrations and their repeat/idempotence behavior were executed on the disposable PostgreSQL proof branch described below; production was not touched.
- Stock writes require the holder business, an administrator, or an active durable scoped delegation. Material identity is source-business scoped; public inventory uses stable `stone_<32 hex>` identifiers rather than passport UUIDs. Inventory or price edits lock the affected rows, clear publication evidence/timestamps, return the BidRock listing to private draft, and require explicit re-publication.
- Profile verification and BidRock entitlement reconciliation now execute atomically and idempotently on the explicit state-transition boundary. Suspended or revoked relationships are not promoted, and JW/BidRock labels are derived from the active relationship and entitlement state.
- Offer, reservation, allocation, and order transitions use database-clock expiry plus row locks/CAS versions. Stone Core `held_quantity` and canonical allocation rows release exactly once on cancel/expiry and consume exactly once on completion. Canonical marketplace/procurement linkage is immutable and one-to-one; linkage and settlement revalidate buyer, seller, listing/order provenance, contents, totals, ACH state, and all canonical fee fields as zero.
- Handoff writes require the seller, an administrator, or an active scoped order delegation. Freight, custody, fabrication, and installation/HomeID transitions require staged provider/reference/evidence, include semantic fields in idempotency fingerprints, and enforce prerequisites with locked/CAS transitions so custody can complete before order completion.
- The routed client now derives counteroffer, accept/reject, cancel, ACH readiness, canonical reconciliation, settlement, handoff, and completion actions from server capabilities. Invalid actions are hidden. Mobile lot selection opens a bounded detail sheet, and BidRock visual tokens are owned by `bidrock-theme.css` rather than inline color literals.
- Final bounded correction adds upgrade handling for the legacy `/u/*`-only `profile_accounts.source_path` constraint, a named validated safe-path constraint that accepts `/jw-stone` and `/bidrock`, and fail-closed schema readiness middleware scoped only to profile-account and BidRock traffic. Optional BidRock listing locks no longer target the nullable side of an outer join; Stone Core rows and their optional listing are locked and re-read separately inside the transaction.
- All external listing and order routes resolve purpose-built `brl_*` and `bro_*` identifiers server-side. Exact read/write/publish inventory delegation scopes remain independent, profile ownership confers no Stone/BidRock mutation authority, and provider workspaces omit internal party, financial, canonical-link, listing UUID, order UUID, and handoff UUID fields.
- Offer/order capabilities use effective database-clock expiry without mutating GET requests. Client mutation retry keys remain stable until success. Handoffs are unique per order/type and move monotonically through pending, in-progress, and completed with version checks and semantic fingerprints. Admin projection, expiry, seven-lot import, and exact handoff-delegation controls plus a mobile seller editor are capability-gated in the routed workspace.

## Final closure evidence

- Canonical Drizzle journal source metadata lists migrations `0115` through `0118` in increasing order; a focused structural test verifies that declaration. The exact SQL files were also applied in that order on a disposable PostgreSQL branch.
- Migration `0118` audits every missing, malformed, or duplicate listing public identifier and deterministically remaps affected rows to canonical `brl_*` identifiers before adding a validated format constraint and ready unique index. The public identifier is covered by the immutable-listing trigger.
- Structural migration tests verify that migration `0118` declares conflict checks and survivor merges for every legacy idempotency fingerprint and every actually present `metadata._bidrockReplayOrderStatuses` entry before duplicate deletion. Migration `0118` executed successfully twice against a production-derived disposable schema; that branch contained no legacy BidRock rows, so the legacy-row survivor merge remains source-contract proof rather than an observed dirty-data migration outcome.
- Startup preflight now checks `convalidated` constraints, `indisunique`/`indisvalid`/`indisready` indexes, and enabled origin/always integrity triggers. Stone inventory, profile-account, and BidRock route families use separate readiness scopes; Stone mutations that also update BidRock require both scopes while unrelated application traffic can still start.
- Focused service/helper and source-contract tests verify that suspended/revoked viewers fail Stone write/publish checks and that active viewers still need the exact holder-business write or publish scope. Existing BidRock price/publication and seller order paths continue to require verified seller access. No live-session authorization journey was run.
- Stock edits, price changes, re-publication, and bulk projection use one lock order across canonical material, passport, position, and listing rows. Projection re-reads after locking, refreshes title/material/source/image/dimensions/finish/quantity facts from Stone Core, increments the listing version with CAS, and keeps edited inventory private until explicit publication.
- Source-order contract tests verify that offer and handoff retry lookup precedes mutable-state prerequisites, and structural migration tests verify preservation of recorded replay maps. No concurrent PostgreSQL retry was executed, so replay behavior is not claimed as database-observed.
- The server queue/workspace DTO now returns privacy-safe per-handoff `nextStatus`, `enabled`, and `disabledReason` capabilities computed from order, payment, custody, and handoff state; internal party IDs, financial values, and canonical links remain absent from the provider DTO. Component tests verify that the provider action button follows the server capability and displays its reason. Provider queue reachability remains browser-unverified.
- `recordBidRockHandoff` now performs successful semantic replay before mutable-state gating, then uses that same server capability after the authoritative order/current-handoff locks and completed-prerequisite read. Every new pending, in-progress, or completed mutation must have an enabled capability and exactly match its `nextStatus` before insert/update. Pure/helper and source-order regressions cover rejection of a new freight handoff after an order reaches fabrication and the valid pending → in-progress → completed sequence; no PostgreSQL transaction execution is claimed.

## Validation evidence

- `node_modules/.bin/prettier --check server/services/bidrockService.ts server/tests/bidrock-last-corrections.contract.test.ts server/tests/bidrock-closure-corrections.contract.test.ts server/tests/bidrock-correction-safety.contract.test.ts .selective-intelligence/builds/bidrock-marketplace-recovery/evidence.md` — passed for every file changed by the final handoff-capability correction. SQL remains validated structurally by focused tests because this Prettier installation has no SQL parser.
- `node_modules/.bin/vitest run server/tests/bidrock-last-corrections.contract.test.ts server/tests/bidrock-closure-corrections.contract.test.ts server/tests/bidrock-final-corrections.contract.test.ts server/tests/bidrock-correction-safety.contract.test.ts client/src/features/bidrock/BidRockOperationsPanels.test.tsx --reporter=dot` — passed at the final handoff-capability correction revision: 5 files and 33 tests passed. This proves pure authorization/capability behavior, source-order mutation gating, structural migration/source contracts, and jsdom component interaction; disposable PostgreSQL proof is recorded separately below, while real-browser reachability remains unproven.
- The opt-in `server/tests/bidrock-postgres.integration.test.ts` harness was pointed at the disposable branch, but this workspace's direct network sandbox failed DNS resolution before any test query ran. The same three database scenarios were then executed through the authenticated Neon database channel against the exact same isolated branch. The local-runner failure is an environment transport limitation, not a passing harness result.
- Disposable database proof used TradeScout Neon child branch `br-icy-haze-ad5gmb2q` only. Migrations `0115` through `0118` applied as 173 ordered statements, and every migration applied successfully a second time. Startup-preflight parity checked 46 required columns, 7 validated constraints, 11 ready unique indexes, and 6 enabled integrity triggers with zero missing guards.
- The legacy `/u/*` account-path scenario created a temporary legacy schema, applied migration `0117`, verified removal of the old constraint, verified the named safe constraint was validated, accepted `/jw-stone` and `/bidrock`, and rejected `//unsafe.example` at the database constraint.
- Two overlapping seven-lot imports plus a third repeat produced exactly 7 passports, 7 inventory positions, 82 slabs, zero duplicate positions, and zero buyer-visible positions. Gold Macaubas remained 6 slabs. This is observed disposable-PostgreSQL state, not a production import.
- Two overlapping full-quantity hold attempts targeted the same six-slab position. One update returned the held row, the other returned no row, and final `held_quantity` equaled `quantity`; the database did not oversubscribe the lot.
- The disposable schema contained no BidRock fee/commission/revenue column, migration `0118` removed the legacy `sold_listing_fee_cents` column, and no proof order or listing row was created. Test-only functions/schema were removed, the held quantity was reset, and the disposable branch was deleted and confirmed absent at `2026-08-21T03:40:18Z`.
- `node_modules/.bin/tsc --noEmit --pretty false` — exited 2 only in unrelated baseline files: `client/src/admin/SuperAdminLeftNav.tsx`, `client/src/pages/admin-live-stream.tsx`, `client/src/pages/homeid/HomeIdWorkspace.tsx`, `server/services/managedPartnerProfileHealth.ts`, `server/services/runtimeManagedPartnerProfileHealth.ts`, and `shared/managedPartnerProfileRegistry.ts`. It reported no BidRock, Stone inventory, profile-account correction, or migration test TypeScript error.
- `npm run build` — passed after the last correction: Vite transformed 4,069 modules, public landing and built-asset URL verification passed, and the server bundle completed. Existing Browserslist, ambiguous duration utility, dynamic-import, and chunk-size warnings remained. Downloaded Red Graniti build cache files were removed and generated sitemap dates were restored after proof.
- `node_modules/.bin/prettier --check server/index.ts server/schemaPreflight.ts server/routes/stone-inventory.ts server/routes/bidrock.ts server/services/stoneInventoryService.ts server/services/bidrockService.ts shared/bidrock.ts client/src/features/bidrock/bidrockClient.ts client/src/features/bidrock/BidRockWorkspace.tsx server/tests/bidrock-closure-corrections.contract.test.ts server/tests/bidrock-marketplace-recovery.contract.test.ts migrations/meta/_journal.json` — passed for all listed closure-correction files.
- `git diff --check` — passed after the correction implementation.
- `git diff --exit-code origin/main -- server/payment-service.ts` — passed with no output; the canonical payment service is byte-identical to `origin/main`.
- `rg -n '\$100|10000|bidrock_(fee|platform_fee)|BIDROCK_[A-Z_]*FEE|marketplace fee' server/services/bidrockService.ts server/routes/bidrock.ts client/src/features/bidrock shared/bidrock.ts migrations/0118_bidrock_marketplace.sql` — returned no matches. The legacy revenue column appears only in the migration's explicit removal, while canonical generic fee fields remain limited to required zero-value linkage/settlement validation.
- `rg -n '#[0-9A-Fa-f]{3,8}' client/src/features/bidrock --glob '*.tsx'` — returned no matches; BidRock client colors are owned by the bounded feature stylesheet.
- The generated project index was refreshed from the complete checkout for the auction-first pass. It reports 213 directories and 3,669 source files; the earlier Start Pack seal is historical, and the auction amendment now proceeds to a fresh Objector review.
- `project_index.py doctor --root . --json` reported `stale: false` but exited 1 on 1,108 pre-existing whole-repository exact-duplicate and competing-export-owner findings plus five raw-element warnings. The comparable `origin/main` index reports 1,115 errors and five warnings. None of the current findings names a BidRock or Stone correction owner; they remain outside this bounded pass and do not invalidate the zero-error Start Pack validation.
- `node --import tsx scripts/backfill-jw-stone-confirmed-stock.ts` — previously passed in dry-run mode; printed all 7 exact lots and `buyerVisibleOnImport: false`; no database connection or mutation occurred.
- `node scripts/proof-profile-native-account.mjs` — not executed as a browser journey because `PROOF_BASE_URL`, disposable proof credentials, phone, and business inputs were not supplied; no external account was created.

## External activation boundaries

- No production backfill has been run.
- Ordered migrations were applied and repeated only on the disposable PostgreSQL branch, which was deleted after proof. They were not applied to production, and the seven-lot production backfill remains separately authorization-gated.
- No production payment intent, ACH mandate, capture, payout, or accounting mutation has been activated.
- On-platform provider processing remains blocked until the canonical marketplace transaction authority persists an authoritative ACH rail and an approved BidRock adapter is configured. The implemented local path records readiness and reconciles only a canonical transaction explicitly identified as ACH.
- The existing temporary Render preview is authorized for redeployment and browser proof; that proof is still pending at this checkpoint.
- No PR, merge, production deploy, production publication, or external payment action has occurred.
