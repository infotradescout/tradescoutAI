# Scope and Release

The smallest complete loop is a standalone `/jw-stone` route where a visitor immediately sees a real header/footer, First Cut, New Arrivals, and supplied catalog under `Current Inventory`; opens truthful details in one click; optionally uses Aesthetic, Color, material, and finish refinements; saves eligible named stones locally; and may deliberately hand a safe selection to Direct Connect with customer role on the request form.

Out of scope / void: customer-path guides, Learn about stone theater, yellow eyebrows, Call-for-availability / Trending Selection marketing, doctrine fact grids on cards, public prices, inventing storefront theater.

Live availability is never inferred from the catalog or recorded source counts. Merge to `main` requires owner local preview GO on the JW marketplace lane (`jw-stone/marketplace-end-user-reset`).
